class GamePlay

    preload: ->
         console.log "Boot::preload()" if debug

    create: ->
                console.log "Boot::preload()" if debug

    update: ->
        console.log "Boot::preload()" if debug

